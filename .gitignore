# Rahasia — private key, token bot, API key. JANGAN PERNAH di-commit.
.env

# State runtime lokal
settings.json
history.json
charts/

# Python
__pycache__/
*.pyc

# Lain-lain
*.log
.playwright-mcp/
*:Zone.Identifier

# cache set posisi aktif (dibangun ulang otomatis)
.active_positions.json
